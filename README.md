# MyMoviePY
This is for my MoviePY application, Mix 2 Music clips and generate sound wave, Layout 2-07 - finished.

# Requirements
https://mac.install.guide/homebrew/3

#Mac OS terminal commands
```
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

```
brew install ffmpeg
brew install cmake
brew install cairo pkg-config
```
Install Packages in PyCharm
```
pip install --upgrade pip setuptools wheel
pip install moviepy
pip install seewav
```
